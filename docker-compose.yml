version: "3.9"
services:
  vulnerable-app:
    build: ./vulnerable-app
    ports:
      - "8080:8080"
    environment:
      - FLASK_ENV=development
      - DEBUG=true
      - DB_PASSWORD=SuperSecret123
